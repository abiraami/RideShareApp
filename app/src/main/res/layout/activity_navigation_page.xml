<?xml version="1.0" encoding="utf-8"?>
<android.support.v4.widget.DrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/drawer_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true"
    android:background="@android:color/white"
    tools:openDrawer="start">

    <include
        layout="@layout/app_bar_navigation_page"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <android.support.design.widget.NavigationView
        android:id="@+id/nav_view"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        android:fitsSystemWindows="true"
        android:theme="@style/NavigationView"
        app:itemTextColor="@android:color/black"
        app:itemIconTint="@android:color/black"
        app:headerLayout="@layout/nav_header_navigation_page"
        app:menu="@menu/activity_navigation_page_drawer"
        android:background="@android:color/white"/>
    <!--
     app:itemTextColor="@color/material_blue_grey_800"
    app:itemIconTint="@color/material_deep_teal_500"
    -->
</android.support.v4.widget.DrawerLayout>
